<img src="../public/assets/img/Asset 1.png" alt="Burger icnon">
<h1>All the burgers that I have devoured!</h1>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <ul>
                {{#each cats}}
                {{#unless sleepy}}
                {{> cats/cat-block sleep=true}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-md-6">
            <ul>
                {{#each burgers}}
                {{#if sleepy}}
                {{> cats/cat-block sleep=false}}
                {{/if}}
                {{/each}}
            </ul>

        </div>
    </div>
</div>