<img class="main-image" src="assets/img/Asset 1.png" alt="Burger icnon">
<h1>Creat a burger!!</h1>

<div class="row">
    <div class="col-md-12">
        <form class="create-form" action="submit">
            <input type="text" placeholder="Enter a burger you want to devoure!">
            <button class="create-button">Create</button>
        </form>
    </div>
</div>
<h2>All the burgers that I have devoured!</h2>
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <ul class="list notDev-list">
                {{#each burger}}
                {{#unless devoured}}
                {{> burger/burger-block devoured=true}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="list col-md-6">
            <ul class="dev-list">
                {{#each burger}}
                {{#if devoured}}
                {{> burger/burger-block devoured=false}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>